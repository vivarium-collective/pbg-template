"""Vendored helpers from pbg-superpowers v0.1.1.

These let pbg-template workspaces drive the canonical PR flow (stages 4-6, 8, 9..N)
without requiring the Claude Code plugin.

Schema paths are workspace-relative — they resolve to <workspace>/.pbg/schemas/.
"""
__version__ = "0.1.4"
